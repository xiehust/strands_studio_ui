# Strands Agent SDK core dependencies
strands-agents>=0.1.0
strands-agents-tools>=0.1.0

# AWS SDK for Python (if using AWS services)
boto3>=1.26.0

# Additional dependencies that might be needed
requests>=2.28.0
httpx>=0.24.0

# OpenAI client (if using OpenAI models)
openai>=1.0.0

# MCP dependencies (if using MCP tools)
mcp>=1.0.0

# JSON handling
pydantic>=2.0.0