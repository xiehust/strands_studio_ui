# AWS Bedrock AgentCore direct-code-deploy runtime dependencies
# (vendored into the deployment zip for aarch64-manylinux2014 / Python 3.13)

# Core AgentCore SDK (entrypoint contract: BedrockAgentCoreApp)
bedrock-agentcore>=1.17.0,<2

# AWS SDK
boto3>=1.40.0
botocore>=1.40.0

# Strands Agent Framework
strands-agents[openai]>=1.46.0
strands-agents-tools>=0.8.3

# MCP client (also a transitive dep of strands-agents; pinned explicitly
# because generated flow code imports it directly)
mcp>=1.0.0

# HTTP (used by generated tools / MCP HTTP transports)
requests>=2.28.0
